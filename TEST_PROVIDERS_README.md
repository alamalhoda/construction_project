# راهنمای تست دسترسی به مدل‌های مختلف

این اسکریپت (`test_all_providers.py`) برای تست دسترسی به مدل‌های مختلف از providerهای مختلف استفاده می‌شود.

## 🎯 هدف

این اسکریپت به شما کمک می‌کند:
- بررسی دسترسی به مدل‌های رایگان و پولی
- تشخیص مشکل تحریم جغرافیایی
- بررسی نیاز به VPN
- پیدا کردن بهترین مدل‌های در دسترس

## 📋 Providerهای پشتیبانی شده

1. **Google Gemini** - مدل‌های رایگان Flash
2. **OpenAI** - مدل‌های GPT (معمولاً پولی)
3. **OpenRouter** - دسترسی یکپارچه به مدل‌های مختلف
4. **Hugging Face** - مدل‌های متن‌باز

## 🚀 نحوه استفاده

### 1. نصب وابستگی‌ها

```bash
# فعال‌سازی محیط مجازی
source env/bin/activate

# نصب کتابخانه‌های مورد نیاز
pip install google-generativeai langchain-openai langchain-huggingface python-dotenv
```

### 2. تنظیم API Keys

اطمینان حاصل کنید که در فایل `.env` API keyهای مورد نیاز را تنظیم کرده‌اید:

```env
# Google Gemini
GOOGLE_API_KEY=your_google_api_key_here

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here

# OpenRouter
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Hugging Face (اختیاری)
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
HUGGINGFACE_ENDPOINT=your_endpoint_here
```

### 3. اجرای اسکریپت

```bash
# اجرای مستقیم
python3 test_all_providers.py

# یا با دسترسی اجرایی
./test_all_providers.py
```

## 📊 خروجی اسکریپت

اسکریپت برای هر provider:
- ✅ مدل‌های رایگان کارآمد را نشان می‌دهد
- 💰 مدل‌های پولی را نشان می‌دهد
- ❌ خطاها و مشکلات را گزارش می‌کند

### مثال خروجی:

```
🔍 تست Google Gemini
✅ API Key found: AIzaSyCk0bG4_asMuw2B...
ℹ️  تست مدل‌های رایگان...
✅ gemini-2.0-flash - کار می‌کند (رایگان)
✅ gemini-2.5-flash - کار می‌کند (رایگان)
```

## ⚠️ مشکلات رایج

### 1. مشکل تحریم جغرافیایی

اگر خطای `User location is not supported` دریافت کردید:
- از VPN استفاده کنید
- یا از OpenRouter استفاده کنید (که از طریق سرورهای خود دسترسی دارد)

### 2. مشکل Quota

اگر خطای `quota exceeded` دریافت کردید:
- برای Google: مدل‌های رایگان محدودیت روزانه دارند
- برای OpenAI: نیاز به افزایش quota
- برای OpenRouter: نیاز به اضافه کردن credit

### 3. مشکل Authentication

اگر خطای `401` یا `Authentication error` دریافت کردید:
- API key را بررسی کنید
- مطمئن شوید که API key معتبر است
- برای OpenRouter: مطمئن شوید که HTTP_REFERER و X_TITLE تنظیم شده‌اند

## 🔄 استفاده منظم

برای بررسی منظم دسترسی، می‌توانید این اسکریپت را:
- قبل از شروع کار اجرا کنید
- در صورت بروز مشکل اجرا کنید
- به صورت دوره‌ای (مثلاً روزانه) اجرا کنید

## 📝 نکات مهم

1. **VPN**: اگر در منطقه تحریم شده هستید، همیشه VPN را فعال کنید
2. **API Keys**: هرگز API keyهای خود را در Git commit نکنید
3. **Rate Limits**: به محدودیت‌های rate limit توجه کنید
4. **Costs**: مدل‌های پولی ممکن است هزینه داشته باشند

## 🛠️ سفارشی‌سازی

می‌توانید اسکریپت را برای اضافه کردن مدل‌های بیشتر یا providerهای جدید سفارشی کنید:

```python
# اضافه کردن مدل جدید به لیست
free_models = [
    'your-new-free-model',
    # ...
]
```

## 📞 پشتیبانی

در صورت بروز مشکل:
1. لاگ‌های خطا را بررسی کنید
2. API keyها را بررسی کنید
3. اتصال اینترنت و VPN را بررسی کنید
4. مستندات provider را مطالعه کنید

