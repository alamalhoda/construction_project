# 🔧 Tool Generator - تولید خودکار Tools از APIs

این ابزار می‌تواند به صورت خودکار Tools مناسب برای AI Assistant از ViewSets و OpenAPI Schema تولید کند.

## 📋 تفاوت RAG و Tools

### RAG (Retrieval-Augmented Generation)
- **هدف**: جستجو در مستندات و کدها
- **استفاده**: وقتی کاربر سوالی درباره API یا مستندات می‌پرسد
- **مثال**: "چطور می‌توانم یک هزینه ایجاد کنم؟"
- **نتیجه**: اطلاعات از مستندات برمی‌گردد

### Tools (ابزارهای قابل اجرا)
- **هدف**: اجرای توابع واقعی
- **استفاده**: وقتی کاربر می‌خواهد عملیاتی انجام دهد
- **مثال**: "یک هزینه 100000 تومانی ایجاد کن"
- **نتیجه**: تابع واقعی اجرا می‌شود و هزینه ایجاد می‌شود

## 🚀 نحوه استفاده

### روش 1: تولید از OpenAPI Schema (پیشنهادی)

```bash
cd /path/to/construction_project
source env/bin/activate
python construction/assistant/tool_generator.py --method schema
```

این روش از فایل `schema.json` استفاده می‌کند که با `drf-spectacular` تولید می‌شود.

### روش 2: تولید از ViewSets

```bash
python construction/assistant/tool_generator.py --method viewset
```

این روش ViewSets را تحلیل می‌کند و Tools تولید می‌کند.

### روش 3: هر دو روش

```bash
python construction/assistant/tool_generator.py --method both
```

### تعیین فایل خروجی

```bash
python construction/assistant/tool_generator.py --method schema --output my_tools.py
```

## 📝 خروجی

فایل تولید شده شامل:
- ✅ توابع با decorator `@tool`
- ✅ Docstring کامل
- ✅ Type hints
- ✅ ساختار اولیه برای پیاده‌سازی

## ⚠️ نکات مهم

1. **Tools تولید شده نیاز به تکمیل دارند**: فقط ساختار اولیه تولید می‌شود
2. **باید منطق واقعی را پیاده‌سازی کنید**: TODO ها را تکمیل کنید
3. **تست کنید**: قبل از استفاده در production، تست کنید
4. **بهینه‌سازی**: می‌توانید Tools را بهینه کنید

## 🔍 مثال خروجی

```python
@tool
def expense_list(request=None) -> str:
    """
    دریافت لیست هزینه‌ها
    
    این Tool از API endpoint GET /api/v1/Expense/ استفاده می‌کند.
    
    Args:
        request: درخواست HTTP برای احراز هویت (برای استفاده داخلی)
    
    Returns:
        نتیجه عملیات به صورت رشته متنی
    """
    try:
        # TODO: پیاده‌سازی فراخوانی API
        return f"✅ درخواست GET به /api/v1/Expense/ ارسال شد"
    except Exception as e:
        return f"❌ خطا: {str(e)}"
```

## 📚 استفاده از Tools تولید شده

بعد از تولید و تکمیل Tools:

1. Tools را در `tools.py` import کنید
2. در `agent.py` اضافه کنید
3. در system prompt معرفی کنید

## 🎯 مزایا

- ✅ صرفه‌جویی در زمان
- ✅ سازگاری با API
- ✅ ساختار یکپارچه
- ✅ مستندسازی خودکار

## 🔄 به‌روزرسانی

هر بار که API تغییر می‌کند:
1. Schema را به‌روزرسانی کنید: `python manage.py spectacular --file schema.json`
2. Tools را دوباره تولید کنید
3. تغییرات را بررسی و اعمال کنید

