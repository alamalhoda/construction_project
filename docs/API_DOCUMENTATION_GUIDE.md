# راهنمای مستندات API

این مستند راهنمای استفاده از مستندات API سیستم مدیریت پروژه‌های ساختمانی است.

## 📋 فهرست مطالب

- [مقدمه](#مقدمه)
- [انواع مستندات API](#انواع-مستندات-api)
- [Swagger UI](#swagger-ui)
- [ReDoc](#redoc)
- [Schema JSON](#schema-json)
- [مقایسه و تفاوت‌ها](#مقایسه-و-تفاوت‌ها)
- [کاربردهای عملی](#کاربردهای-عملی)
- [دسترسی](#دسترسی)

---

## مقدمه

سیستم مدیریت پروژه‌های ساختمانی از کتابخانه `drf-spectacular` برای تولید خودکار مستندات API استفاده می‌کند. این مستندات به سه شکل مختلف در دسترس است:

1. **Swagger UI** - رابط کاربری تعاملی
2. **ReDoc** - رابط کاربری جایگزین
3. **Schema JSON** - فایل JSON خام

---

## انواع مستندات API

### Swagger UI

**آدرس:** `/api/swagger-ui/`

#### ویژگی‌ها:
- ✅ رابط کاربری گرافیکی و تعاملی
- ✅ امکان تست مستقیم API از داخل مرورگر
- ✅ نمایش کامل endpoint‌ها، پارامترها و پاسخ‌ها
- ✅ پشتیبانی از احراز هویت (Authentication)
- ✅ امکان ارسال درخواست و مشاهده پاسخ به صورت زنده
- ✅ جستجو و فیلتر endpoint‌ها

#### کاربردها:
- **تست API**: تست سریع و آسان endpoint‌ها بدون نیاز به ابزارهای خارجی
- **مستندسازی**: مشاهده مستندات کامل API به صورت بصری
- **یادگیری**: درک سریع ساختار و نحوه استفاده از API
- **توسعه**: استفاده در مرحله توسعه برای بررسی صحت API

#### مناسب برای:
- توسعه‌دهندگان Frontend
- تست‌کنندگان API
- تیم‌های توسعه که نیاز به تست سریع دارند
- مستندسازی برای کاربران نهایی

---

### ReDoc

**آدرس:** `/api/redoc/`

#### ویژگی‌ها:
- ✅ رابط کاربری زیبا و خوانا
- ✅ طراحی responsive و موبایل‌فرندلی
- ✅ نمایش مستندات به صورت عمودی و ساده
- ✅ جستجوی پیشرفته در مستندات
- ✅ نمایش مثال‌های کد
- ✅ پشتیبانی از چندین زبان

#### کاربردها:
- **مستندسازی عمومی**: نمایش مستندات برای کاربران غیرفنی
- **مرجع سریع**: دسترسی سریع به اطلاعات endpoint‌ها
- **اشتراک‌گذاری**: به اشتراک گذاری مستندات با تیم یا مشتریان

#### مناسب برای:
- تیم‌های غیرفنی که نیاز به درک API دارند
- مستندسازی عمومی
- نمایش در موبایل و تبلت

#### تفاوت با Swagger UI:
- **ReDoc**: فقط برای مشاهده و خواندن مستندات (بدون قابلیت تست)
- **Swagger UI**: علاوه بر مشاهده، امکان تست API را نیز فراهم می‌کند

---

### Schema JSON

**آدرس:** `/api/schema/`

#### ویژگی‌ها:
- ✅ فایل JSON خام و ساختاریافته
- ✅ فرمت استاندارد OpenAPI 3.0
- ✅ قابل استفاده در ابزارهای مختلف
- ✅ قابل import در Postman، Insomnia و سایر ابزارها
- ✅ قابل استفاده برای تولید خودکار کد کلاینت

#### کاربردها:

##### 1. **تولید خودکار کد کلاینت**
```bash
# استفاده با openapi-generator
openapi-generator generate -i /api/schema/ -g python -o ./client
```

##### 2. **Import در Postman**
- باز کردن Postman
- Import → Link
- وارد کردن آدرس `/api/schema/`
- تمام endpoint‌ها به صورت خودکار اضافه می‌شوند

##### 3. **تست خودکار با CI/CD**
```yaml
# مثال GitHub Actions
- name: Validate API Schema
  run: |
    curl -o schema.json http://localhost:8000/api/schema/
    npx @stoplight/spectral-cli lint schema.json
```

##### 4. **تولید مستندات سفارشی**
- استفاده از ابزارهایی مانند `redoc-cli` برای تولید HTML
- استفاده از `swagger-codegen` برای تولید کد

##### 5. **یکپارچه‌سازی با ابزارها**
- Jira برای مستندسازی
- Confluence برای مستندات تیم
- ابزارهای مانیتورینگ API

#### مناسب برای:
- توسعه‌دهندگان Backend
- تیم‌های DevOps
- ابزارهای CI/CD
- تولید خودکار کد
- یکپارچه‌سازی با سیستم‌های دیگر

---

## مقایسه و تفاوت‌ها

| ویژگی | Swagger UI | ReDoc | Schema JSON |
|-------|------------|-------|-------------|
| **نوع** | رابط کاربری | رابط کاربری | فایل JSON |
| **تست API** | ✅ بله | ❌ خیر | ❌ خیر |
| **محتوا** | یکسان | یکسان | یکسان |
| **استفاده انسان** | ✅ عالی | ✅ خوب | ❌ مناسب نیست |
| **استفاده ماشینی** | ❌ خیر | ❌ خیر | ✅ عالی |
| **Import در ابزارها** | ❌ خیر | ❌ خیر | ✅ بله |
| **تولید کد خودکار** | ❌ خیر | ❌ خیر | ✅ بله |
| **CI/CD** | ❌ خیر | ❌ خیر | ✅ بله |
| **موبایل‌فرندلی** | ⚠️ متوسط | ✅ عالی | ✅ بله |
| **جستجو** | ✅ بله | ✅ بله | ❌ خیر |

---

## کاربردهای عملی

### سناریو 1: توسعه Frontend

**نیاز:** توسعه‌دهنده Frontend می‌خواهد API را تست کند و ببیند چه پارامترهایی نیاز است.

**راه‌حل:** استفاده از **Swagger UI**
- باز کردن `/api/swagger-ui/`
- پیدا کردن endpoint مورد نظر
- تست مستقیم از داخل مرورگر
- مشاهده پاسخ و ساختار داده

### سناریو 2: تست خودکار

**نیاز:** تیم می‌خواهد تست‌های خودکار برای API بنویسد.

**راه‌حل:** استفاده از **Schema JSON**
- دریافت schema از `/api/schema/`
- استفاده از ابزارهایی مانند `openapi-generator`
- تولید خودکار تست‌ها

### سناریو 3: مستندسازی برای مشتری

**نیاز:** ارائه مستندات API به مشتری غیرفنی.

**راه‌حل:** استفاده از **ReDoc**
- رابط کاربری ساده و خوانا
- مناسب برای نمایش به افراد غیرفنی
- طراحی زیبا و حرفه‌ای

### سناریو 4: یکپارچه‌سازی با Postman

**نیاز:** تیم از Postman برای تست API استفاده می‌کند.

**راه‌حل:** استفاده از **Schema JSON**
- Import schema در Postman
- تمام endpoint‌ها به صورت خودکار اضافه می‌شوند
- صرفه‌جویی در زمان

### سناریو 5: تولید کد کلاینت

**نیاز:** تولید خودکار کد کلاینت برای زبان‌های مختلف.

**راه‌حل:** استفاده از **Schema JSON**
```bash
# Python
openapi-generator generate -i /api/schema/ -g python -o ./python-client

# JavaScript
openapi-generator generate -i /api/schema/ -g javascript -o ./js-client

# TypeScript
openapi-generator generate -i /api/schema/ -g typescript-axios -o ./ts-client
```

---

## تولید خودکار کد کلاینت با OpenAPI Generator

### OpenAPI Generator چیست؟

`openapi-generator` یک ابزار قدرتمند است که از فایل Schema JSON (OpenAPI Specification) استفاده می‌کند و به صورت خودکار کد کلاینت برای زبان‌های مختلف برنامه‌نویسی تولید می‌کند.

#### مزایا:
- ✅ **صرفه‌جویی در زمان**: نیازی به نوشتن دستی کد کلاینت نیست
- ✅ **کاهش خطا**: کد تولید شده بر اساس Schema دقیق است
- ✅ **همگام‌سازی**: با تغییر API، کد کلاینت به‌روز می‌شود
- ✅ **پشتیبانی از 40+ زبان**: Python, JavaScript, Java, C#, Go و...

---

### نصب OpenAPI Generator

#### روش 1: استفاده از npm (پیشنهادی)
```bash
npm install -g @openapitools/openapi-generator-cli
```

#### روش 2: استفاده از Homebrew (macOS)
```bash
brew install openapi-generator
```

#### روش 3: استفاده از Docker
```bash
docker pull openapitools/openapi-generator-cli
```

---

### ساختار دستور

```bash
openapi-generator generate \
  -i <ورودی> \          # آدرس Schema JSON
  -g <زبان> \            # نوع زبان خروجی
  -o <خروجی>            # پوشه خروجی
```

#### پارامترها:

| پارامتر | توضیح | مثال |
|---------|-------|------|
| `-i` یا `--input-spec` | آدرس Schema JSON (URL یا مسیر فایل) | `http://localhost:8000/api/schema/` |
| `-g` یا `--generator-name` | نام generator (زبان خروجی) | `python`, `javascript`, `typescript-axios` |
| `-o` یا `--output` | پوشه خروجی برای کد تولید شده | `./client` یا `./python-client` |

---

### مثال‌های عملی

#### 1. تولید کلاینت Python

```bash
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g python \
  -o ./python-client
```

**خروجی:**
```
python-client/
├── openapi_client/
│   ├── __init__.py
│   ├── api/
│   │   ├── expense_api.py
│   │   ├── investor_api.py
│   │   └── transaction_api.py
│   ├── models/
│   │   ├── expense.py
│   │   └── investor.py
│   └── configuration.py
├── setup.py
└── README.md
```

**استفاده در کد Python:**
```python
from openapi_client import ApiClient, Configuration
from openapi_client.api.expense_api import ExpenseApi

# تنظیمات
configuration = Configuration(host="http://localhost:8000")
api_client = ApiClient(configuration)

# استفاده از API
expense_api = ExpenseApi(api_client)
expenses = expense_api.expense_list()
```

---

#### 2. تولید کلاینت JavaScript

```bash
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g javascript \
  -o ./javascript-client
```

**خروجی:**
```
javascript-client/
├── src/
│   ├── api/
│   │   ├── ExpenseApi.js
│   │   └── InvestorApi.js
│   └── model/
│       ├── Expense.js
│       └── Investor.js
├── package.json
└── README.md
```

**استفاده در کد JavaScript:**
```javascript
import { ExpenseApi, ApiClient } from './javascript-client';

const apiClient = new ApiClient();
apiClient.basePath = 'http://localhost:8000';

const expenseApi = new ExpenseApi(apiClient);
expenseApi.expenseList().then(expenses => {
    console.log(expenses);
});
```

---

#### 3. تولید کلاینت TypeScript

```bash
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g typescript-axios \
  -o ./typescript-client
```

**استفاده:**
```typescript
import { ExpenseApi, Configuration } from './typescript-client';

const config = new Configuration({
    basePath: 'http://localhost:8000'
});

const expenseApi = new ExpenseApi(config);
const expenses = await expenseApi.expenseList();
```

---

### زبان‌های پشتیبانی شده

| زبان | نام Generator | مثال |
|------|---------------|------|
| **Python** | `python` | `-g python` |
| **JavaScript** | `javascript` | `-g javascript` |
| **TypeScript** | `typescript-axios` | `-g typescript-axios` |
| **Java** | `java` | `-g java` |
| **C#** | `csharp` | `-g csharp` |
| **Go** | `go` | `-g go` |
| **PHP** | `php` | `-g php` |
| **Ruby** | `ruby` | `-g ruby` |
| **Swift** | `swift5` | `-g swift5` |
| **Kotlin** | `kotlin` | `-g kotlin` |

**لیست کامل:** [openapi-generator generators](https://openapi-generator.tech/docs/generators/)

---

### گزینه‌های پیشرفته

#### 1. تنظیمات اضافی

```bash
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g python \
  -o ./python-client \
  --package-name construction_api \    # نام پکیج
  --git-user-id arash-project \         # برای Git
  --git-repo-id construction-api-client
```

#### 2. استفاده از فایل محلی

```bash
# دانلود Schema
curl -o schema.json http://localhost:8000/api/schema/

# استفاده از فایل محلی
openapi-generator generate \
  -i ./schema.json \
  -g python \
  -o ./python-client
```

#### 3. استفاده با Docker

```bash
docker run --rm \
  -v ${PWD}:/local \
  openapitools/openapi-generator-cli generate \
  -i http://host.docker.internal:8000/api/schema/ \
  -g python \
  -o /local/python-client
```

---

### به‌روزرسانی کلاینت

وقتی API تغییر می‌کند:

```bash
# 1. حذف کلاینت قدیمی
rm -rf ./python-client

# 2. تولید مجدد
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g python \
  -o ./python-client
```

**یا استفاده از Git:**
```bash
# تولید در یک branch جدید
git checkout -b update-client
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g python \
  -o ./python-client

# مقایسه تغییرات
git diff
```

---

### مثال کامل: استفاده در پروژه

#### مرحله 1: تولید کلاینت
```bash
cd /path/to/your/project
openapi-generator generate \
  -i http://localhost:8000/api/schema/ \
  -g python \
  -o ./api_client
```

#### مرحله 2: نصب در پروژه
```bash
cd api_client
pip install -e .
```

#### مرحله 3: استفاده در کد
```python
# main.py
from api_client import ExpenseApi, ApiClient, Configuration

# تنظیمات
config = Configuration(
    host="http://localhost:8000",
    username="your_username",
    password="your_password"
)

# استفاده
with ApiClient(config) as api_client:
    expense_api = ExpenseApi(api_client)
    
    # دریافت لیست هزینه‌ها
    expenses = expense_api.expense_list()
    print(f"تعداد هزینه‌ها: {len(expenses)}")
    
    # ایجاد هزینه جدید
    new_expense = {
        "amount": "1000000",
        "expense_type": "project_manager",
        "period": 1,
        "project": 1
    }
    created = expense_api.expense_create(new_expense)
    print(f"هزینه جدید ایجاد شد: {created.id}")
```

---

### نکات مهم

#### ✅ مزایا:
- کد تولید شده استاندارد و قابل اعتماد است
- صرفه‌جویی قابل توجه در زمان توسعه
- همگام‌سازی خودکار با تغییرات API

#### ⚠️ محدودیت‌ها:
- کد تولید شده ممکن است نیاز به تنظیمات اضافی داشته باشد
- برای API‌های پیچیده ممکن است نیاز به ویرایش دستی باشد
- برخی generatorها ممکن است کامل نباشند

#### 💡 بهترین روش‌ها:
- همیشه کد تولید شده را بررسی کنید
- از version control استفاده کنید
- تست‌های واحد برای کلاینت بنویسید
- در صورت نیاز، کد را extend کنید (نه modify مستقیم)

---

## دسترسی

### دسترسی از داشبورد

برای مدیران فنی، لینک‌های دسترسی در بخش **"مستندات API"** در داشبورد موجود است.

### دسترسی مستقیم

- **Swagger UI**: `http://localhost:8000/api/swagger-ui/`
- **ReDoc**: `http://localhost:8000/api/redoc/`
- **Schema JSON**: `http://localhost:8000/api/schema/`

### احراز هویت

برای تست API در Swagger UI:
1. ابتدا باید وارد سیستم شوید
2. در Swagger UI، روی دکمه **"Authorize"** کلیک کنید
3. توکن احراز هویت خود را وارد کنید (در صورت استفاده از Token Authentication)

---

## نکات مهم

### ⚠️ امنیت
- در محیط Production، دسترسی به مستندات API را محدود کنید
- از احراز هویت برای محافظت از endpoint‌ها استفاده کنید

### 🔄 به‌روزرسانی
- Schema به صورت خودکار از کد Django تولید می‌شود
- هر تغییر در API به صورت خودکار در مستندات منعکس می‌شود

### 📝 بهترین روش‌ها
- برای تست سریع: از **Swagger UI** استفاده کنید
- برای مستندسازی عمومی: از **ReDoc** استفاده کنید
- برای یکپارچه‌سازی: از **Schema JSON** استفاده کنید

---

## منابع بیشتر

- [مستندات drf-spectacular](https://drf-spectacular.readthedocs.io/)
- [مستندات OpenAPI](https://swagger.io/specification/)
- [راهنمای Swagger UI](https://swagger.io/tools/swagger-ui/)
- [راهنمای ReDoc](https://github.com/Redocly/redoc)

---

**آخرین به‌روزرسانی:** 2024
**نسخه API:** 1.0.0

