# اسکریپت‌های تست دستیار هوش مصنوعی

این پوشه شامل اسکریپت‌های تست برای سیستم دستیار هوش مصنوعی پروژه است.

## 📋 فایل‌های موجود

### 1. `test_agent_with_llm.py`
اسکریپت تست Agent با LLM واقعی. این اسکریپت Agent را با LLM واقعی (OpenRouter) تست می‌کند و سوالات واقعی از آن می‌پرسد.

**استفاده:**
```bash
source env/bin/activate
python3 assistant/scripts/test_agent_with_llm.py
```

### 2. `test_llm_providers.py`
اسکریپت تست جامع برای بررسی وضعیت تمام مدل‌های LLM. این اسکریپت تمام provider های موجود را تست می‌کند و وضعیت آن‌ها را گزارش می‌دهد.

**استفاده:**
```bash
source env/bin/activate
python3 assistant/scripts/test_llm_providers.py
```

### 3. `test_all_providers.py`
اسکریپت جامع برای تست دسترسی به مدل‌های مختلف از providerهای مختلف. این اسکریپت دسترسی به مدل‌های رایگان و غیر رایگان را بررسی می‌کند.

**استفاده:**
```bash
source env/bin/activate
python3 assistant/scripts/test_all_providers.py
```

## 📚 مستندات

برای اطلاعات بیشتر درباره تست دسترسی به مدل‌های مختلف، به فایل زیر مراجعه کنید:
- `assistant/docs/TEST_PROVIDERS_README.md`

## ⚙️ پیش‌نیازها

قبل از اجرای این اسکریپت‌ها، مطمئن شوید که:

1. محیط مجازی فعال است:
   ```bash
   source env/bin/activate
   ```

2. متغیرهای محیطی در فایل `.env` تنظیم شده‌اند:
   - `OPENROUTER_API_KEY` (برای test_agent_with_llm.py)
   - `GOOGLE_API_KEY` یا `GEMINI_API_KEY` (برای تست Gemini)
   - `OPENAI_API_KEY` (برای تست OpenAI)
   - سایر API keyهای مورد نیاز

3. وابستگی‌های پروژه نصب شده‌اند:
   ```bash
   pip install -r requirements.txt
   ```

## 🔍 توضیحات

این اسکریپت‌ها برای تست و بررسی عملکرد سیستم دستیار هوش مصنوعی طراحی شده‌اند. آن‌ها به شما کمک می‌کنند:

- بررسی دسترسی به مدل‌های مختلف LLM
- تست عملکرد Agent با سوالات واقعی
- تشخیص مشکلات اتصال یا احراز هویت
- پیدا کردن بهترین مدل‌های در دسترس

## 📝 نکات مهم

- این اسکریپت‌ها ممکن است از API keyهای شما استفاده کنند و هزینه‌ای داشته باشند
- برای تست‌های طولانی، از مدل‌های رایگان استفاده کنید
- در صورت بروز مشکل، لاگ‌های خطا را بررسی کنید

