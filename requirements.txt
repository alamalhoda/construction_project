# Excel processing libraries
openpyxl>=3.1.0
pandas>=2.0.0
xlrd>=2.0.1

# Date and time handling
jdatetime==5.2.0
django-jalali==7.4.0
pytz>=2023.3

# Data manipulation
numpy>=1.24.0

# Optional: For better Excel support
xlsxwriter>=3.1.0

# Development dependencies (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# django
Django==4.2.23
# ------
django-filter>=25.0.0
django-extensions==4.1
djangorestframework==3.16.1

# Production dependencies
gunicorn>=21.2.0
whitenoise>=6.6.0
django-csp>=3.7

python-dotenv==1.1.1

# PostgreSQL support (اختیاری)
# psycopg2-binary>=2.9.7

# Redis for caching (اختیاری)
# django-redis>=5.4.0

# AI Assistant Service - فقط برای ارتباط با سرویس دستیار
httpx>=0.25.0
PyJWT>=2.8.0

# برای تولید OpenAPI Schema (نیاز به drf-spectacular)
drf-spectacular>=0.27.0

# توجه: وابستگی‌های AI (langchain, chromadb, mcp) به برنامه دستیار منتقل شده‌اند
# برای استفاده از دستیار، باید برنامه دستیار را اجرا کنید

# HTTP client dependencies
urllib3>=1.24.2,<2.4.0
