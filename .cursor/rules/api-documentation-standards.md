# 📚 قانون استانداردهای مستندات API

## 🎯 هدف
این قانون استانداردهای مستندات API را تعریف می‌کند که برای بهینه‌سازی استفاده از LLM و RAG system طراحی شده است.

**Single Source of Truth**: Serializer با help_text صریح

---

## 🚫 **قوانین ممنوع**

### ❌ **تکرار اطلاعات در docstrings و serializer ممنوع است**
- جزئیات فیلدها فقط در serializer با `help_text` تعریف می‌شوند
- docstrings در API فقط شامل خلاصه، مثال و نکات کلیدی هستند

### ❌ **docstrings طولانی در API methods ممنوع است**
- docstrings نباید بیش از 20-25 خط باشند
- جزئیات فنی فیلدها در serializer هستند

### ❌ **استفاده از extra_kwargs برای help_text ممنوع است**
- فیلدها باید به صورت صریح تعریف شوند تا help_text به schema اضافه شود

---

## ✅ **قوانین اجباری**

### 1. **Serializer: Single Source of Truth**

#### ✅ **تعریف صریح فیلدها با help_text**
```python
class ExpenseSerializer(serializers.ModelSerializer):
    # تعریف صریح فیلدها برای اطمینان از اضافه شدن help_text به schema
    id = serializers.IntegerField(
        read_only=True, 
        help_text='شناسه یکتای هزینه (فقط خواندنی)'
    )
    period = serializers.PrimaryKeyRelatedField(
        queryset=models.Period.objects.all(),
        required=True,
        help_text='شناسه دوره‌ای که هزینه برای آن ثبت می‌شود. دوره باید متعلق به پروژه جاری باشد. (الزامی)'
    )
    expense_type = serializers.ChoiceField(
        choices=models.Expense.EXPENSE_TYPES,
        required=True,
        help_text='نوع هزینه. مقادیر معتبر: project_manager (مدیر پروژه), facilities_manager (سرپرست کارگاه), procurement (کارپرداز), warehouse (انباردار), construction_contractor (پیمان ساختمان), other (سایر). (الزامی)'
    )
    amount = serializers.DecimalField(
        max_digits=20,
        decimal_places=2,
        required=True,
        help_text='مبلغ هزینه به تومان. باید مقدار مثبت باشد. برای جلوگیری از مشکلات دقت، به صورت string ارسال شود. (الزامی)'
    )
    description = serializers.CharField(
        required=False,
        allow_blank=True,
        help_text='توضیحات تکمیلی درباره هزینه. حداکثر 500 کاراکتر. (اختیاری)'
    )
```

#### ❌ **اشتباه: استفاده از extra_kwargs**
```python
# ❌ اشتباه - help_text ممکن است به schema اضافه نشود
class Meta:
    extra_kwargs = {
        'amount': {
            'help_text': 'مبلغ هزینه'
        }
    }
```

#### ✅ **فرمت help_text**
- شامل توضیح کامل فیلد
- شامل الزامی/اختیاری بودن
- شامل مقادیر معتبر (برای enum ها)
- شامل محدودیت‌ها و validation rules
- شامل مثال‌های کاربردی (در صورت نیاز)

---

### 2. **API Docstrings: کوتاه و کاربردی**

#### ✅ **ساختار docstring برای CRUD methods**
```python
def list(self, request, *args, **kwargs):
    """
    دریافت لیست تمام هزینه‌های پروژه جاری

    این متد لیست هزینه‌های مرتبط با پروژه فعال را برمی‌گرداند.
    نتایج به صورت صفحه‌بندی شده و قابل مرتب‌سازی هستند.

    Query Parameters:
        - page: شماره صفحه (پیش‌فرض: 1)
        - page_size: تعداد رکورد در هر صفحه (پیش‌فرض: 10)
        - ordering: فیلد مرتب‌سازی با پیشوند "-" برای نزولی (پیش‌فرض: -created_at)

    Returns:
        Response با ساختار paginated شامل results, count, next, previous

    مثال:
        GET /api/v1/Expense/?page=1&page_size=20&ordering=-amount

    نکات:
        - فقط هزینه‌های پروژه جاری برگردانده می‌شود
        - نیاز به احراز هویت دارد
    """
    return super().list(request, *args, **kwargs)
```

#### ✅ **ساختار docstring برای create/update**
```python
def create(self, request, *args, **kwargs):
    """
    ایجاد هزینه جدید برای پروژه جاری

    این متد هزینه جدید را برای پروژه فعال ثبت می‌کند.
    پروژه به صورت خودکار از session کاربر تعیین می‌شود.

    Request Body:
        - period (الزامی): شناسه دوره متعلق به پروژه جاری
        - expense_type (الزامی): نوع هزینه (project_manager, facilities_manager, procurement, warehouse, construction_contractor, other)
        - amount (الزامی): مبلغ هزینه به تومان (به صورت string)
        - description (اختیاری): توضیحات تکمیلی

    Returns:
        Response با اطلاعات هزینه ایجاد شده (status 201)

    مثال:
        POST /api/v1/Expense/
        {
            "period": 1,
            "expense_type": "project_manager",
            "amount": "5000000",
            "description": "حقوق مدیر پروژه برای ماه دسامبر"
        }

    نکات:
        - جزئیات فیلدها در serializer descriptions موجود است
        - پروژه به صورت خودکار از session تنظیم می‌شود
        - نیاز به احراز هویت و دسترسی APISecurityPermission دارد
    """
    return super().create(request, *args, **kwargs)
```

#### ❌ **اشتباه: docstring طولانی با جزئیات فیلدها**
```python
# ❌ اشتباه - جزئیات فیلدها باید در serializer باشند
def create(self, request, *args, **kwargs):
    """
    ایجاد هزینه جدید
    
    Request Body:
        1. period (دوره - شناسه)
           - نوع: Integer
           - اجباری: بله
           - توضیح: شناسه دوره‌ای که هزینه برای آن ثبت می‌شود...
           [جزئیات کامل که باید در serializer باشد]
    """
```

---

### 3. **ViewSet Class Docstring: جامع و کاربردی**

#### ✅ **ساختار docstring برای ViewSet**
```python
class ExpenseViewSet(ProjectFilterMixin, viewsets.ModelViewSet):
    """
    ViewSet برای مدیریت هزینه‌های پروژه
    
    این ViewSet امکان مدیریت کامل هزینه‌های پروژه را فراهم می‌کند.
    
    قابلیت‌ها:
    - ایجاد، خواندن، به‌روزرسانی و حذف هزینه‌ها
    - دریافت آمار و گزارش‌های مالی
    - محاسبه مجموع هزینه‌ها بر اساس نوع و دوره
    - مدیریت هزینه‌های دوره‌ای
    
    سناریوهای استفاده:
    - ثبت هزینه‌های مواد اولیه
    - ثبت هزینه‌های نیروی کار
    - ثبت هزینه‌های اداری و عمومی
    - دریافت گزارش‌های مالی برای تحلیل پروژه
    
    مثال‌های کاربرد:
    - برای ثبت خرید سیمان و آجر: expense_type='project_manager', amount='5000000'
    - برای دریافت لیست تمام هزینه‌ها: GET /api/v1/Expense/
    
    نکات مهم:
    - تمام عملیات بر اساس پروژه جاری (active project) انجام می‌شود
    - هزینه‌ها می‌توانند به یک دوره خاص مرتبط باشند
    - انواع هزینه: project_manager, facilities_manager, procurement, warehouse, construction_contractor, other
    """
```

---

## 📋 **چک‌لیست برای هر ViewSet جدید**

### Serializer (serializers.py)
- [ ] همه فیلدها به صورت صریح تعریف شده‌اند
- [ ] هر فیلد دارای `help_text` کامل است
- [ ] `help_text` شامل: توضیح، الزامی/اختیاری، مقادیر معتبر، محدودیت‌ها
- [ ] برای enum fields، تمام مقادیر معتبر در help_text ذکر شده‌اند

### ViewSet (api.py)
- [ ] ViewSet class دارای docstring جامع است
- [ ] هر CRUD method دارای docstring کوتاه است
- [ ] docstrings شامل: خلاصه، پارامترها، Returns، مثال، نکات
- [ ] مثال‌های عملی در docstrings موجود است
- [ ] docstrings بیش از 25 خط نیستند

### Schema Generation
- [ ] بعد از تغییرات، schema جدید تولید شده است
- [ ] `help_text` ها در schema موجود هستند
- [ ] docstrings در schema موجود هستند

---

## 🔄 **فرآیند به‌روزرسانی**

### 1. **هنگام ایجاد ViewSet جدید**
1. Serializer را با فیلدهای صریح و help_text کامل ایجاد کنید
2. ViewSet class docstring را کامل کنید
3. CRUD methods docstrings را با مثال اضافه کنید
4. Schema را تولید کنید: `python3 manage.py spectacular --file schema.json`
5. بررسی کنید که help_text ها در schema موجود هستند

### 2. **هنگام تغییر فیلدها**
1. فقط Serializer را تغییر دهید (Single Source of Truth)
2. Schema را دوباره تولید کنید
3. docstrings API را فقط در صورت تغییر behavior به‌روزرسانی کنید

### 3. **هنگام تغییر behavior**
1. docstrings مربوطه را به‌روزرسانی کنید
2. مثال‌ها را در صورت نیاز به‌روزرسانی کنید
3. Schema را دوباره تولید کنید

---

## 🎯 **بهینه‌سازی برای LLM و RAG**

### ✅ **مزایای این رویکرد**
1. **Single Source of Truth**: تغییرات فقط در یک جا (Serializer)
2. **بهینه برای LLM**: توضیحات کامل + مثال‌های عملی
3. **بهینه برای RAG**: اطلاعات ساختاریافته در schema
4. **نگهداری آسان**: تغییرات فقط در Serializer

### 📊 **اطلاعات در Schema**
- **Properties**: help_text از Serializer
- **Endpoints**: docstrings از API methods
- **Examples**: مثال‌های عملی در docstrings

### 🔍 **اطلاعات در RAG Documents**
- **Field Descriptions**: از schema properties
- **Endpoint Descriptions**: از schema endpoints
- **Examples**: از docstrings
- **Usage Scenarios**: از ViewSet class docstring

---

## ⚠️ **هشدارها**

- **عدم رعایت قوانین** منجر به مستندات ناقص برای LLM می‌شود
- **تکرار اطلاعات** منجر به inconsistency می‌شود
- **docstrings طولانی** خوانایی کد را کاهش می‌دهد
- **فراموش کردن تولید schema** منجر به مستندات قدیمی می‌شود

---

## 📝 **مثال کامل**

### Serializer (serializers.py)
```python
class ExpenseSerializer(serializers.ModelSerializer):
    period_data = PeriodSerializer(source='period', read_only=True)
    period_weight = serializers.SerializerMethodField()
    
    # تعریف صریح فیلدها
    id = serializers.IntegerField(
        read_only=True, 
        help_text='شناسه یکتای هزینه (فقط خواندنی)'
    )
    period = serializers.PrimaryKeyRelatedField(
        queryset=models.Period.objects.all(),
        required=True,
        help_text='شناسه دوره‌ای که هزینه برای آن ثبت می‌شود. دوره باید متعلق به پروژه جاری باشد. (الزامی)'
    )
    expense_type = serializers.ChoiceField(
        choices=models.Expense.EXPENSE_TYPES,
        required=True,
        help_text='نوع هزینه. مقادیر معتبر: project_manager (مدیر پروژه), facilities_manager (سرپرست کارگاه), procurement (کارپرداز), warehouse (انباردار), construction_contractor (پیمان ساختمان), other (سایر). (الزامی)'
    )
    amount = serializers.DecimalField(
        max_digits=20,
        decimal_places=2,
        required=True,
        help_text='مبلغ هزینه به تومان. باید مقدار مثبت باشد. برای جلوگیری از مشکلات دقت، به صورت string ارسال شود. (الزامی)'
    )
    description = serializers.CharField(
        required=False,
        allow_blank=True,
        help_text='توضیحات تکمیلی درباره هزینه. حداکثر 500 کاراکتر. (اختیاری)'
    )
    created_at = serializers.DateTimeField(
        read_only=True, 
        help_text='تاریخ و زمان ایجاد هزینه (فقط خواندنی)'
    )

    class Meta:
        model = models.Expense
        fields = [
            "id", "project", "expense_type", "amount", "description",
            "period", "period_data", "period_weight", "created_at",
        ]
```

### ViewSet (api.py)
```python
class ExpenseViewSet(ProjectFilterMixin, viewsets.ModelViewSet):
    """
    ViewSet برای مدیریت هزینه‌های پروژه
    
    این ViewSet امکان مدیریت کامل هزینه‌های پروژه را فراهم می‌کند.
    
    قابلیت‌ها:
    - ایجاد، خواندن، به‌روزرسانی و حذف هزینه‌ها
    - دریافت آمار و گزارش‌های مالی
    
    نکات مهم:
    - تمام عملیات بر اساس پروژه جاری انجام می‌شود
    """
    
    def create(self, request, *args, **kwargs):
        """
        ایجاد هزینه جدید برای پروژه جاری

        این متد هزینه جدید را برای پروژه فعال ثبت می‌کند.

        Request Body:
            - period (الزامی): شناسه دوره
            - expense_type (الزامی): نوع هزینه
            - amount (الزامی): مبلغ هزینه
            - description (اختیاری): توضیحات

        Returns:
            Response با اطلاعات هزینه ایجاد شده (status 201)

        مثال:
            POST /api/v1/Expense/
            {
                "period": 1,
                "expense_type": "project_manager",
                "amount": "5000000",
                "description": "حقوق مدیر پروژه"
            }

        نکات:
            - جزئیات فیلدها در serializer descriptions موجود است
            - نیاز به احراز هویت دارد
        """
        return super().create(request, *args, **kwargs)
```

---

**تاریخ ایجاد**: 2025-01-09  
**نسخه**: 1.0  
**وضعیت**: فعال  
**هدف**: بهینه‌سازی مستندات API برای LLM و RAG system
